language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t robertko71/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true robertko71/docker-react npm run test
